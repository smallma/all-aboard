=== codex inbox（claude → codex） ===

說明：本檔由 claude 寫入任務、codex 讀取執行。
完工後請在對應 TASK 末尾標 STATUS: DONE 並補 RESULT 行。
若有問題或要回覆 claude，請寫到 claude-inbox.txt。

⚠️ 每次接任務前必讀：docs/agents/coding-conventions.md
⚠️ 詳細協作流程：docs/agents/README.txt

================================================================
== 已歸檔完成任務（不需重做） ==
================================================================

[歸檔] TASK #001 素材搬移 — STATUS: DONE (APPROVED by claude 2026-05-26 17:00)
       詳見 docs/agents/archive/codex-inbox-2026-05-26-snapshot.txt

[歸檔] TASK #002 量測座位座標 — STATUS: DONE (APPROVED by claude 2026-05-26 16:55)
       詳見 docs/agents/archive/codex-inbox-2026-05-26-snapshot.txt

================================================================
== 目前待辦任務（請依序執行） ==
================================================================

⚠️ 重要：以下任務都是 STATUS: PENDING。請從 #003 開始依序執行。
   每完成一個就在該 task 末尾追加 STATUS: DONE + RESULT，然後
   在 claude-inbox.txt 追加 [DONE TASK #NNN] 通知 claude。
   做完一個任務就結束本輪，下輪再做下一個。

=== TASK #003 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/shared/Avatar.tsx
PRIORITY: high
CREATED: 2026-05-26
SPEC: design §3 模組劃分 / §2.5 範本資料 avatarId
INTERFACE:
  type Props = {
    avatarId: string;
    size?: number;       // 預設 40，單位 px
    className?: string;  // 可額外覆蓋
  };
  export function Avatar(props: Props): JSX.Element;
DEPENDENCIES:
  - 用 src/lib/constants.ts 的 avatarSrc(avatarId), AVATAR_LIST 與 FALLBACK_AVATAR_ID
  - 用 clsx 組 className
  - 圖片來源：public/avatars/{avatarId}.png（已搬移完畢）
ACCEPTANCE:
  - [ ] named export，函式元件
  - [ ] avatarId 在 AVATAR_LIST 內 → 渲染 /avatars/{avatarId}.png
  - [ ] avatarId 不在清單 → 自動 fallback 到 FALLBACK_AVATAR_ID（不報錯）
  - [ ] 使用 Next.js Image 元件（next/image），src 用 avatarSrc()，width/height = size
  - [ ] 預設 className 包含 rounded-full + object-cover + overflow-hidden
  - [ ] 可被 props.className 覆蓋
  - [ ] npx tsc --noEmit 通過
---
規格：

import Image from 'next/image';
import { clsx } from 'clsx';
import { AVATAR_LIST, FALLBACK_AVATAR_ID, avatarSrc } from '@/lib/constants';

接受 avatarId 並做 fallback，size 預設 40（行內顯示用）。

注意：
- 不要直接寫 <img>，用 next/image
- next/image 需要 width 和 height（用 size prop 同時設定）
- 路徑用 avatarSrc() 取得，不要 hardcode
- 全部用 named export
- 不要寫 default export
- 不寫 // TODO，遇到問題標 BLOCKED
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/shared/Avatar.tsx (新建)：Next Image avatar 元件，支援 AVATAR_LIST 驗證、fallback、size 與 className。

=== TASK #004 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/canvas/DriverSeat.tsx
PRIORITY: medium
CREATED: 2026-05-26
SPEC: design §4.3 駕駛防呆 / §3 模組劃分
INTERFACE:
  type Props = {
    carId: string;
    driverId: string | null;
    xPct: number;
    yPct: number;
  };
  export function DriverSeat(props: Props): JSX.Element;
DEPENDENCIES:
  - useDroppable from '@dnd-kit/core'
  - buildDndId from '@/lib/dnd-id'  → buildDndId({ kind: 'driver', carId })
  - 業務邏輯（canDrive 防呆）不在這層做，由 dragdrop action 處理
ACCEPTANCE:
  - [ ] named export
  - [ ] 用 useDroppable，id = buildDndId({ kind: 'driver', carId })
  - [ ] 容器 className 包含 absolute + rounded-full
  - [ ] style 用 left/top 百分比定位
  - [ ] -translate-x-1/2 + -translate-y-1/2 對齊中心
  - [ ] 容器大小：w-12 h-12
  - [ ] driverId 為 null：淺灰虛線邊框圓圈 + lucide Car icon (text-slate-400)
  - [ ] driverId 非 null：背景 amber-100 + 占位文字 "已坐"
  - [ ] isOver === true：ring-2 ring-blue-400
  - [ ] npx tsc --noEmit 通過
---
範例骨架（你可調整）：

import { useDroppable } from '@dnd-kit/core';
import { Car } from 'lucide-react';
import { clsx } from 'clsx';
import { buildDndId } from '@/lib/dnd-id';

type Props = { carId: string; driverId: string | null; xPct: number; yPct: number };

export function DriverSeat({ carId, driverId, xPct, yPct }: Props) {
  const { setNodeRef, isOver } = useDroppable({
    id: buildDndId({ kind: 'driver', carId }),
  });
  return (
    <div
      ref={setNodeRef}
      style={{ left: `${xPct}%`, top: `${yPct}%` }}
      className={clsx(
        'absolute -translate-x-1/2 -translate-y-1/2',
        'w-12 h-12 rounded-full flex items-center justify-center',
        driverId ? 'bg-amber-100' : 'border-2 border-dashed border-slate-300',
        isOver && 'ring-2 ring-blue-400',
      )}
    >
      {driverId ? <span className="text-xs">已坐</span> : <Car className="w-5 h-5 text-slate-400" />}
    </div>
  );
}
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/canvas/DriverSeat.tsx (新建)：dnd-kit 駕駛座 droppable 元件，支援百分比定位、空/已坐狀態與 isOver ring。

=== TASK #005 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/canvas/SeatSlot.tsx
PRIORITY: medium
CREATED: 2026-05-26
SPEC: design §4.4 座位衝突
INTERFACE:
  type Props = {
    carId: string;
    seatIndex: number;
    passengerId: string | null;
    xPct: number;
    yPct: number;
    label: string;        // aria-label 用
  };
  export function SeatSlot(props: Props): JSX.Element;
DEPENDENCIES:
  - useDroppable from '@dnd-kit/core'
  - buildDndId({ kind: 'seat', carId, seatIndex })
ACCEPTANCE:
  - [ ] named export
  - [ ] 同 TASK #004 的 droppable + 絕對定位 + 中心對齊
  - [ ] 容器大小 w-10 h-10
  - [ ] passengerId 為 null：虛線 border-slate-300 淡灰圓圈占位
  - [ ] passengerId 非 null：bg-slate-200 實心圓圈 + 文字「坐」
  - [ ] isOver：ring-2 ring-blue-400
  - [ ] aria-label={label}
  - [ ] tsc 通過
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/canvas/SeatSlot.tsx (新建)：dnd-kit 座位 droppable 元件，支援百分比定位、空/已坐狀態、aria-label 與 isOver ring。

=== TASK #006 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/canvas/Trunk.tsx
PRIORITY: medium
CREATED: 2026-05-26
SPEC: design §4.5 後車廂
INTERFACE:
  type Props = {
    carId: string;
    itemIds: string[];    // 僅用於計算數量徽章
    xPct: number;
    yPct: number;
    wPct: number;
    hPct: number;
  };
  export function Trunk(props: Props): JSX.Element;
DEPENDENCIES:
  - useDroppable from '@dnd-kit/core'
  - buildDndId({ kind: 'trunk', carId })
  - lucide Package icon
ACCEPTANCE:
  - [ ] named export
  - [ ] 絕對定位 left/top/width/height (% 全部)
  - [ ] -translate-x-1/2 -translate-y-1/2 對齊中心
  - [ ] 容器：rounded-xl bg-slate-200/60 border-2 border-dashed border-slate-300
  - [ ] 內部置中：Package icon (text-slate-400) + 物品數量徽章（itemIds.length > 0 才顯示）
  - [ ] isOver：ring-2 ring-blue-400
  - [ ] tsc 通過
---
注意：
- 不渲染物品本身，僅占位 + 數量
- 大小由父層傳 wPct/hPct 決定
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/canvas/Trunk.tsx (新建)：dnd-kit 後車廂 droppable 元件，支援百分比定位、占位 icon、物品數量徽章與 isOver ring。

=== TASK #007 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/canvas/OwnerBadge.tsx
PRIORITY: high
CREATED: 2026-05-27
SPEC: design §4.7 車主招牌 / §5 動畫清單
INTERFACE:
  type Props = {
    title: string;       // 「Rain 的車」或「請指派司機」字串（由父層計算）
    onClick?: () => void;
  };
  export function OwnerBadge(props: Props): JSX.Element;
DEPENDENCIES:
  - framer-motion 的 motion + AnimatePresence
  - clsx
ACCEPTANCE:
  - [ ] named export
  - [ ] 用 AnimatePresence + motion.span（key={title}），title 變化時 spring 進場
  - [ ] 樣式：rounded-full bg-white shadow px-3 py-1 text-sm font-medium border border-slate-200
  - [ ] title === "請指派司機" → text-slate-400；其他 → text-slate-900
  - [ ] 可選 onClick（有 onClick 時加 cursor-pointer + hover:shadow-md）
  - [ ] tsc 通過
---
範例：

<AnimatePresence mode="wait">
  <motion.span
    key={title}
    initial={{ y: -8, opacity: 0, scale: 0.9 }}
    animate={{ y: 0, opacity: 1, scale: 1 }}
    exit={{ y: 4, opacity: 0 }}
    transition={{ type: 'spring', stiffness: 260, damping: 22 }}
    className={...}
    onClick={onClick}
  >
    {title}
  </motion.span>
</AnimatePresence>
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/canvas/OwnerBadge.tsx (新建)：framer-motion 車主招牌元件，支援 title 變化 spring 進場、請指派司機淡色狀態與可選 onClick hover 樣式。

=== TASK #008 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/staging/PassengerChip.tsx
PRIORITY: high
CREATED: 2026-05-27
SPEC: design §4.6.1 PassengerChip 三種 placement
INTERFACE:
  type Placement = 'staging' | 'driver' | 'seat';
  type Props = {
    passenger: Passenger;          // from @/lib/types
    placement: Placement;
    onRemove?: () => void;
  };
  export function PassengerChip(props: Props): JSX.Element;
DEPENDENCIES:
  - @/components/shared/Avatar
  - @dnd-kit/core 的 useDraggable（id = buildDndId({ kind:'passenger', passengerId: passenger.id })）
  - @/lib/dnd-id
  - lucide-react 的 X
  - clsx
ACCEPTANCE:
  - [ ] named export
  - [ ] useDraggable 註冊
  - [ ] placement='staging'：
        - inline-flex 圓角標籤、bg-white border border-slate-200、px-2 py-1 gap-2 rounded-full
        - Avatar size=28 + 名字（text-sm）
        - canDrive=true 時名字右側加「💳」emoji（不是 lucide）
  - [ ] placement='driver'：
        - 不自己 absolute（父層處理定位）
        - Avatar size=36 + 👨‍✈️ emoji 疊在 avatar 右上角（absolute -top-1 -right-1 text-base）
        - 名字在 Avatar 下方，text-[10px] text-slate-700，最多 3 字加省略號（用 truncate + max-w-[3em]）
  - [ ] placement='seat'：
        - 同 driver 但無 👨‍✈️，Avatar size=32
  - [ ] placement !== 'staging' 時右上角顯示 ✕ 按鈕 (w-4 h-4)：
        - hover 時顯示
        - 點擊：onRemove?.()，必須 stopPropagation
        - aria-label="移除"
  - [ ] 拖曳中 useDraggable 的 transform 套用 style，opacity-50
  - [ ] tsc 通過
---
備註：
- 不做業務邏輯，✕ 只呼叫 props.onRemove
- driver placement 視覺差異只是視覺，定位由父層 absolute 包住
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/staging/PassengerChip.tsx (新建)：dnd-kit 乘客拖曳元件，支援 staging/driver/seat 三種視覺、driver emoji、非 staging 移除按鈕與拖曳 opacity/transform。

=== TASK #009 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/staging/ItemChip.tsx
PRIORITY: medium
CREATED: 2026-05-27
SPEC: design §4.5 後車廂顯示
INTERFACE:
  type Placement = 'staging' | 'trunk';
  type Props = {
    item: Item;
    placement: Placement;
    onRemove?: () => void;
  };
  export function ItemChip(props: Props): JSX.Element;
DEPENDENCIES:
  - useDraggable，id = buildDndId({ kind:'item', itemId: item.id })
  - lucide Package + X
ACCEPTANCE:
  - [ ] named export
  - [ ] useDraggable
  - [ ] placement='staging'：
        - inline-flex bg-white border border-slate-200 rounded-md px-2 py-1 gap-2
        - Package icon (w-4 h-4 text-slate-500) + 名字 text-sm
  - [ ] placement='trunk'：
        - block w-full bg-white border border-slate-200 rounded-md px-2 py-1.5 flex gap-2 items-center
        - Package icon + 名字 + （若 onRemove 提供）右側 ✕ 按鈕
  - [ ] 拖曳中：transform + opacity-50
  - [ ] ✕ 點擊：stopPropagation + onRemove?.()
  - [ ] tsc 通過
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/staging/ItemChip.tsx (新建)：dnd-kit 物品拖曳元件，支援 staging/trunk 兩種視覺、trunk 移除按鈕與拖曳 opacity/transform。

=== TASK #010 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/staging/AvatarPicker.tsx
PRIORITY: medium
CREATED: 2026-05-27
SPEC: design §4.9 乘客 CRUD（avatar 選擇）
INTERFACE:
  type Props = {
    value: string;
    onChange: (avatarId: string) => void;
  };
  export function AvatarPicker(props: Props): JSX.Element;
DEPENDENCIES:
  - @/lib/constants 的 AVATAR_LIST
  - @/components/shared/Avatar
  - clsx
ACCEPTANCE:
  - [ ] named export
  - [ ] grid grid-cols-6 sm:grid-cols-8 gap-2，48 張 Avatar
  - [ ] 每張 Avatar size=48，<button title={avatarId}>
  - [ ] 選中項 (value === avatarId)：ring-2 ring-blue-500
  - [ ] 點擊 → onChange(avatarId)
  - [ ] 容器：max-h-80 overflow-y-auto + p-2
  - [ ] tsc 通過
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/staging/AvatarPicker.tsx (新建)：48 張頭像選擇器元件，支援目前選取 ring、button title、點擊 onChange 與可捲動 grid。

================================================================
== 監聽指示 ==
================================================================

=== TASK #011 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/editor/EmptyCanvasHint.tsx
PRIORITY: low
CREATED: 2026-05-27
SPEC: design §7 邊界情況：空狀態 + §5 動畫
INTERFACE:
  type Props = {
    onAddCarHint?: () => void;
  };
  export function EmptyCanvasHint(props: Props): JSX.Element;
DEPENDENCIES:
  - framer-motion motion (上下漂浮的小動畫)
  - lucide-react Car icon
ACCEPTANCE:
  - [ ] named export
  - [ ] 'use client'
  - [ ] 容器 flex flex-col items-center justify-center text-center text-slate-400 gap-2 py-12
  - [ ] 用 motion.div animate={{ y: [0, -8, 0] }} transition={{ duration: 2.5, repeat: Infinity, ease: 'easeInOut' }} 包住 Car icon
  - [ ] Car icon 使用 className="w-16 h-16 text-slate-300"
  - [ ] 顯示文字 "畫布是空的"（text-lg）+ "從左側「+ 新增車輛」開始打包" (text-sm)
  - [ ] 若 onAddCarHint 有：底下加一個 button 「立即新增車輛」 (px-4 py-2 rounded-lg bg-blue-600 text-white text-sm)，點擊呼叫 onAddCarHint
  - [ ] tsc 通過
---
範例：

'use client';
import { motion } from 'framer-motion';
import { Car } from 'lucide-react';

export function EmptyCanvasHint({ onAddCarHint }: Props) {
  return (
    <div className="flex flex-col items-center justify-center text-center text-slate-400 gap-2 py-12">
      <motion.div animate={{ y: [0, -8, 0] }} transition={{ duration: 2.5, repeat: Infinity, ease: 'easeInOut' }}>
        <Car className="w-16 h-16 text-slate-300" />
      </motion.div>
      <p className="text-lg">畫布是空的</p>
      <p className="text-sm">從左側「+ 新增車輛」開始打包</p>
      {onAddCarHint && (
        <button type="button" onClick={onAddCarHint} className="mt-4 px-4 py-2 rounded-lg bg-blue-600 text-white text-sm">
          立即新增車輛
        </button>
      )}
    </div>
  );
}
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/editor/EmptyCanvasHint.tsx (新建)：空畫布提示元件，支援 framer-motion 上下漂浮 Car icon、空狀態文案與可選立即新增車輛按鈕。

=== TASK #012 ===
FROM: claude
TO: codex
TOPIC: 實作 src/components/shared/SectionHeader.tsx（小型共用元件）
PRIORITY: low
CREATED: 2026-05-27
SPEC: 內部 UI 統一
INTERFACE:
  type Props = {
    title: string;
    count?: number;             // 若有就顯示 (N) 在 title 後
    action?: React.ReactNode;   // 右側按鈕區（如「+ 新增」）
  };
  export function SectionHeader(props: Props): JSX.Element;
DEPENDENCIES:
  - clsx
ACCEPTANCE:
  - [ ] named export
  - [ ] 容器：flex items-center justify-between mb-2 px-1
  - [ ] 左側：text-xs font-semibold text-slate-500 顯示 title，若 count!=undefined 則 title 後加「（N）」
  - [ ] 右側：render action 節點（若提供）
  - [ ] tsc 通過
---
注意：
- 純呈現元件，不涉及 dnd-kit 或 framer-motion
- 不寫 default export
---
STATUS: PENDING
STATUS: DONE
RESULT:
  - src/components/shared/SectionHeader.tsx (新建)：共用 section header 呈現元件，支援 title、count 顯示與右側 action 節點。

================================================================
== 監聽指示 ==
================================================================

請持續輪詢此檔案。當你看到下面這一整行（claude 會在所有 phase 完成後寫入），就觸發停止流程：

  >>> ALL_TASKS_COMPLETE_STOP_MONITORING <<<

注意：必須是「>>> ALL_TASKS_COMPLETE_STOP_MONITORING <<<」這一整行才算終止信號。


================================================================
== 收工信號 ==
================================================================

所有 phase 已完成，部署 URL：https://all-aboard-three.vercel.app
感謝 codex 的協作！下方為終止信號，shell loop 會偵測到並退出：

>>> ALL_TASKS_COMPLETE_STOP_MONITORING <<<
